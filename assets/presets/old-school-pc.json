{
	"name": "Old school computer",
	"description": "A retro look inspired by old school computer graphics. Optimal retro look highly depends on an stride and size blur config. For bigger images more size and more stride, for tinier images less size and less stride. Works great if both params have the same values, but experiment for yourself",
	"pipeline": [
		{
			"filter": "bnw"
		},
		{
			"filter": "blur",
			"params": {
				"size": 10,
				"stride": 10
			}
		},
		{
			"filter": "FSDithering",
			"params": {
				"depth": 8,
				"amount": 1,
				"perceptual": true,
				"noise": 0.2
			}
		}
	],
	"output_suffix": "_processed",
	"output_extension": "auto"
}
