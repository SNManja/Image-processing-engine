{
	"name": "Old school computer",
	"pipeline": [
		{
			"filter": "bnw"
		},
		{
			"filter": "blur",
			"params": {
				"size": 10,
				"stride": 10
			}
		},
		{
			"filter": "FSDithering",
			"params": {
				"depth": 8,
				"amount": 1,
				"perceptual": true,
				"noise": 0.2
			}
		}
	],
	"output_suffix": "_processed",
	"Recommendations for this filter": "Optimal retro look highly depends on an stride and size blur config. For bigger images more size and more stride, for tinier images less size and less stride. Works great if both params have the same values, but experiment for yourself"
}
